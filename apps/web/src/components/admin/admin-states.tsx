export function AdminLoading() { return <div className="admin-skeleton" aria-label="Загрузка"><span /><span /><span /><span /></div>; }
export function AdminError({ error, retry }: { error: Error; retry: () => void }) { return <div className="admin-state admin-state-error" role="alert"><strong>Не удалось загрузить данные</strong><p>{error.message}</p><button className="ghost" type="button" onClick={retry}>Повторить</button></div>; }
export function AdminEmpty({ title = "Здесь пока нет данных", body = "Измените период или фильтры. Новые события появятся автоматически." }: { title?: string; body?: string }) { return <div className="admin-state"><strong>{title}</strong><p>{body}</p></div>; }
export function Money({ value, unknown = false }: { value?: string | null; unknown?: boolean }) { if (unknown || value == null) return <span className="admin-unknown">Нет данных</span>; return <>{new Intl.NumberFormat("ru-RU", { maximumFractionDigits: 2 }).format(Number(value))} ₽</>; }
export function DateTime({ value }: { value?: string | null }) { return value ? <time dateTime={value}>{new Intl.DateTimeFormat("ru-RU", { dateStyle: "short", timeStyle: "short", timeZone: "Europe/Moscow" }).format(new Date(value))}</time> : <span className="admin-muted">Не было</span>; }
